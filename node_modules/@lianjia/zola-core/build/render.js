'use strict';

var ReactDOM = require('react-dom');
var getType = require('./utils/getType');

module.exports = function render(reactElement, container) {

  if (getType(container) === 'string') container = document.querySelector(container);
  return new Promise(function (resolve) {
    return ReactDOM.render(reactElement, container, resolve);
  });
};