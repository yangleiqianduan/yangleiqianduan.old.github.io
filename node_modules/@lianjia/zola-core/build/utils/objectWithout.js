"use strict";

module.exports = function (object) {
  var result = Object.assign({}, object);

  for (var _len = arguments.length, withouts = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    withouts[_key - 1] = arguments[_key];
  }

  withouts.forEach(function (key) {
    return delete result[key];
  });
  return result;
};