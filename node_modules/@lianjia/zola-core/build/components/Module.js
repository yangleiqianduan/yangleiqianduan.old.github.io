'use strict';

var propTypes = require('prop-types');
var inherit = require('../utils/inherit');
var Base = require('./Base');

var Module = inherit('Module', Base, {});
Module.contextTypes = {
  subscribe: propTypes.object,
  location: propTypes.object
};

module.exports = Module;