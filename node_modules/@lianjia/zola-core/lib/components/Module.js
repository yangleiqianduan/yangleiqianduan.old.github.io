const propTypes = require('prop-types')
const inherit = require('../utils/inherit')
const Base = require('./Base')

const Module = inherit('Module', Base, {})
Module.contextTypes = {
  subscribe: propTypes.object,
  location: propTypes.object
}

module.exports = Module